apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-discovery-hyper-service
spec:
  selector:
    app: {{ .Release.Name }}-discovery-hyper
  ports:
    - name: {{ .Release.Name }}-d-h-s-p
      port: 80
      targetPort: {{ .Release.Name }}-d-h-c-p
      protocol: TCP